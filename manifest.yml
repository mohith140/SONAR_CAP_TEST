---
applications:
  - name: git-cap-app          # your app name (must be unique in CF space)
    path: .                      # current directory
    memory: 512M
    buildpacks:
      - nodejs_buildpack
    command: npm start
